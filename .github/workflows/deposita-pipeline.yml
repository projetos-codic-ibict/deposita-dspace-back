name: Pipeline - Deploy Deposita

on:
  push:
    branches:
      - main

env:
  SUDO_PASSWORD: ${{ secrets.SUDO_PASSWORD }}

jobs:
  DEV:
    runs-on: [self-hosted, linux, x64, dev]
    env:
      ENVIRONMENT: DEV
    environment:
      name: DEV
      url: 'http://172.16.24.33:8080/server'
    
    steps:
      - run: |
          cd /usr/local/dspace/dspace-updater-tool/source/DSpace-dspace-7.6 && git pull
          cd /usr/local/dspace/dspace-updater-tool/source/DSpace-dspace-7.6 && sh ../../update-restart-backend.sh

  # PRD:
  #   runs-on: [self-hosted, linux, x64, prd]
  #   env:
  #     ENVIRONMENT: PRD
  #   environment: 
  #     name: PRD
  #     url: 'https://deposita.ibict.br'
  #   needs: DEV

  #   steps:
  #     - uses: actions/checkout@v3
  #     - run: |
  #         sh ../../update-restart-backend.sh
